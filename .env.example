# Copie para .env e preencha. O .env não vai para o git.

# --- Cadeia de provedores de IA (app/providers/) ---
# Ordem de PRIORIDADE: o primeiro que responder vence. Provider sem chave sai
# da cadeia sozinho, entao para rodar so com o Gemini basta deixar as outras
# chaves em branco. Reordenar ou desligar um provedor e edicao daqui, sem
# deploy de codigo.
LLM_PROVIDERS=gemini,huggingface,groq,openrouter

# 1) Gemini — https://aistudio.google.com/apikey
# GOOGLE_API_KEY continua valendo (nome legado); com as duas preenchidas,
# GEMINI_API_KEY vence.
GEMINI_API_KEY=
GOOGLE_API_KEY=

# 2) HuggingFace Inference Providers — usa o MESMO HF_TOKEN de baixo (o que ja
# baixa o modelo local de embeddings). Sem chave nova: e o fallback "de graca"
# antes de Groq/OpenRouter.
HF_MODEL=meta-llama/Llama-3.3-70B-Instruct

# 3) Groq — https://console.groq.com/keys
GROQ_API_KEY=
# Só o nome do modelo, SEM o prefixo `groq:` — ele vai direto como model name
# para a API (ver providers/chain). O prefixo existe só na sintaxe do override
# `--modelo groq:<nome>` do scripts.ask/eval_run. Confira o catálogo da SUA
# chave com `python -m scripts.modelos --provider groq`.
GROQ_MODEL=groq/compound

# 4) OpenRouter — https://openrouter.ai/keys
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-chat-v3.1

# Modelo fora do catalogo da sua chave da erro 404 ("does not exist OR you do
# not have access to it" — a mensagem nao distingue os dois casos). Veja o que
# a SUA chave acessa, e qual linha daqui esta errada:
#     python -m scripts.modelos
# A cadeia nao cai por causa disso: o provider com modelo invalido sai de cena
# e o proximo responde. Mas a linha sai em ERROR no log, de proposito.

# Permite que o corpo do POST /ask traga `modelo` ([provider:]modelo) e escolha
# com que modelo responder — sem fallback e sem cache. DESLIGADO por padrao: a
# chave e nossa, entao quem escolhe o modelo escolhe o mais caro do catalogo, e
# RATE_LIMIT_DIARIO_* conta requisicoes, nao custo. Ligue em desenvolvimento.
# A CLI (scripts.ask --modelo) nao passa por este switch.
ASK_MODELO_OVERRIDE_ENABLED=false

# Com --modelo/modelo o cache fica DESLIGADO por padrao (nem le nem grava) —
# repetir a mesma pergunta com o mesmo override paga o LLM de novo toda vez.
# Ligue para que um override repetido sirva do cache. Seguro: o MODELO entra
# na chave do cache, entao um override nunca serve a resposta cacheada de
# outro modelo nem da cadeia normal.
MODELO_OVERRIDE_CACHE_ENABLED=false

# https://huggingface.co/settings/tokens — usado para baixar o modelo local de
# embeddings (rate limit maior; sem token, só um pouco mais devagar) E como
# credencial do provider `huggingface` da cadeia de chat acima (2 usos, 1 chave).
# Sem token, os embeddings continuam funcionando e o provider `huggingface`
# simplesmente sai da cadeia.
HF_TOKEN=

# Conexão com o Postgres + pgvector. Local: bate com o docker-compose.yml.
# Supabase: use a Session pooler (IPv4, porta 5432; usuário postgres.<ref>); a
# conexão direta db.<ref>.supabase.co é IPv6-only. Encode caracteres especiais
# da senha (ex.: '@' -> %40).
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5434/agente_ead

# Opcional: a MESMA conexão em formato libpq (sem o `+psycopg`), usada só pelo
# psql e por scripts/psql.ps1 para rodar query no terminal. Não é lida pela app.
SUPABASE_DB_URL=

# Embeddings rodam localmente (HuggingFace/sentence-transformers), sem cota de
# API. e5-base aceita 512 tokens — o antigo paraphrase-multilingual-mpnet-base-v2
# truncava em 128, e 34,4% dos chunks deste corpus passavam disso (medido: média
# 108 tokens, máximo 254). Trocar de modelo muda o espaço vetorial: reingestão
# obrigatória (python -m scripts.ingest) e COLLECTION_NAME novo.
EMBEDDING_MODEL=intfloat/multilingual-e5-base
# Prefixos de instrução do e5 — obrigatórios para este modelo, que é treinado com
# pergunta e documento marcados de formas diferentes. Sem eles não há erro, só
# ranking pior. Deixe os DOIS vazios ao trocar por um modelo sem instrução
# (BAAI/bge-m3, sentence-transformers/paraphrase-*).
EMBEDDING_QUERY_PREFIX="query: "
EMBEDDING_PASSAGE_PREFIX="passage: "
# Modelo do Gemini (o primeiro da cadeia). Os outros dois em GROQ_MODEL e
# OPENROUTER_MODEL, la em cima.
CHAT_MODEL=gemini-3.6-flash

# Chunking
# CHUNK_SIZE em caracteres. So tem efeito para BAIXO (o splitter nunca junta
# paginas de PDF). RET-5: 700 ~= p75 do tamanho das paginas do corpus (mediana
# 403), medido com `python -m scripts.chunk_stats`. Mudou? Reingira.
CHUNK_SIZE=700
CHUNK_OVERLAP=150
# RET-6: na ingestao, descarta chunk com Jaccard (shingles de 4 palavras) >= isso
# contra outro chunk ja mantido do mesmo lote (pega quase-copia que o hash exato
# nao pega - ex. mesma pagina repetida em PDF grande). 0 desliga.
INGEST_DEDUP_SIMILARIDADE=0.9
# Teto de caracteres por fonte de contexto (chunk da base ou snippet da web) ao
# montar o prompt. Uma pagina densa de PDF entra como 1 chunk (>8 mil chars) e
# 5 desses estouram o limite de token por requisicao de provider free-tier
# (HTTP 413). ~6000 ≈ 1500 tokens; 0 desliga.
PROMPT_CONTEXT_ITEM_MAX_CHARS=6000

# Retrieval
TOP_K=5
# Abaixo desse score de relevância (0..1) o agente responde "não encontrei na base".
# Só vale no caminho bi-encoder (RERANKER_ENABLED=false).
RELEVANCE_THRESHOLD=0.85

# --- Reranker cross-encoder (RET-3, app/retrieval/reranker.py) ---
# false = retrieval bi-encoder puro (o de hoje). true = 2 estágios: o E5 traz
# RERANKER_CANDIDATES, o cross-encoder reordena e o corte passa a ser
# RERANKER_THRESHOLD. Ligar só depois de calibrar contra a suíte de fidelidade
# (T-1) — ver eval/future_feature/cross-encoder.md §6.
RERANKER_ENABLED=false
# Multilingue (~120MB), pergunta PT x documento EN (guias Canvas). Modelo local,
# offline-first. bge-reranker-v2-m3 e mais forte porem 2.3GB e ~1-2s/rerank.
RERANKER_MODEL=cross-encoder/mmarco-mMiniLMv2-L12-H384-v1
# k do 1o estagio quando o reranker esta ligado (recall amplo p/ reordenar).
RERANKER_CANDIDATES=30
# Limiar sobre o score do cross-encoder (sigmoid, 0..1) — escala DIFERENTE do
# ~0.82 do E5. 0.0 = nao corta (TOP_K ainda limita); o valor real sai da T-1.
RERANKER_THRESHOLD=0.0

# Cache PRE-RETRIEVAL (app/db/pre_retrieval_cache.py): um hit numa pergunta ja
# respondida pela base devolve a resposta sem tocar em pgvector nem no
# cross-encoder — mata o retrieve() inteiro, nao so a chamada ao LLM. Chave =
# pergunta normalizada + assunto (sem ids de chunk), entao cada reingestao limpa
# a tabela. Subordinado a CACHE_ENABLED; ignorado no canal eval e com
# --modelo/modelo. Desligue ao iterar em prompts.py.
PRE_RETRIEVAL_CACHE_ENABLED=true

# Triagem por assunto: a pergunta de outro departamento (cobranca, diploma,
# rematricula...) e encaminhada com o contato certo ANTES do retrieval, sem
# gastar RAG nem LLM. Categorias e e-mails em ENCAMINHAMENTOS
# (app/core/config.py). Com false, a pergunta volta a seguir para o RAG.
TRIAGEM_ENABLED=true

# Guardrail de entrada: o pedido de ataque/abuso (injecao de prompt, "system
# prompt", "chave de API", "DROP TABLE", "altere a nota"...) e encaminhado para
# o suporte ANTES da triagem e do retrieval, sem tocar RAG/web/LLM. Casamento
# lexico em app/agent/guardrail.py. Com false, volta a seguir para o RAG.
GUARDRAIL_ENABLED=true

# Fallback de busca externa: quando o retrieval nao devolve nada, o agente
# procura nos dominios publicos oficiais (allowlist em app/core/config.py)
# antes de encaminhar para a secretaria. Com false, volta ao comportamento
# anterior: responde "nao encontrei na base" e para por ai.
WEB_FALLBACK_ENABLED=true
# Orcamento total da busca, em segundos (estourou, usa o que ja chegou)
WEB_SEARCH_TIMEOUT=8.0
# Similaridade minima (0..1) entre pergunta e snippet para chegar ao LLM
WEB_RELEVANCE_THRESHOLD=0.80

# Telemetria: 1 linha JSON por pergunta. Os dois destinos sao independentes.
# Mostra o log no terminal (stderr). Com false, a gravacao no banco continua.
TELEMETRY_STDERR_ENABLED=true
# Grava na tabela `telemetria` do Postgres. Com false, so o log no terminal.
TELEMETRY_DB_ENABLED=true
# Retencao dos registros, em dias. Expirados sao apagados na escrita.
TELEMETRY_RETENTION_DAYS=7
# Retencao PROPRIA do canal `eval` (rodadas de scripts.eval_run). Dataset
# sintetico, sem PII de aluno — pode ficar mais tempo, e e o que da ao
# dashboard de /revisao uma serie temporal com sentido.
TELEMETRY_RETENTION_DAYS_EVAL=90

# --- Borda HTTP: seguranca (Sprint 2) ---
# Autenticacao por chave no header X-API-Key. Com true e API_KEYS vazia, as
# rotas /v1/ask e /v1/assuntos respondem 503 (fail-closed) — nunca liberam.
API_AUTH_ENABLED=true
# Uma chave POR INTEGRACAO: nome:chave,outro:chave. O nome vai para a
# telemetria (canal), o que permite atribuir custo e revogar so um consumidor.
# Gerar com: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEYS=

# Origens permitidas no CORS, separadas por virgula. Vazio = nenhuma chamada
# cruzada (a demo servida pela propria API nao precisa). "*" e ignorado.
CORS_ORIGINS=

# Rate limit: janela deslizante de 60s por consumidor...
RATE_LIMIT_POR_MINUTO=20
# ...e teto de custo do processo inteiro por dia (UTC).
RATE_LIMIT_DIARIO_GLOBAL=2000

# Onde os contadores de rate limit vivem. VAZIO = memoria (correto so com 1
# worker do uvicorn; com N workers os tetos viram N*limite). Setar para um
# Redis compartilhado quando subir mais de um worker/replica. Redis fora do ar
# => a requisicao passa com WARNING (nao 500). Ex.: redis://localhost:6379/0
REDIS_URL=

# Timeout POR PROVIDER, em segundos, e quantas tentativas em cada um antes de
# cair para o proximo. Tentativas=1 de proposito: com tres provedores em fila,
# insistir com quem acabou de devolver 429 so gasta o tempo do aluno — fallback
# e perguntar a outro, retry e insistir com o mesmo.
# Pior caso de espera = LLM_TIMEOUT * nº de providers * LLM_TENTATIVAS_POR_PROVIDER.
LLM_TIMEOUT=30.0
LLM_TENTATIVAS_POR_PROVIDER=1
# Teto de tokens de SAIDA por resposta, aplicado em todo provider (VET-3).
# Resposta de suporte cabe em ~800-1000; 1400 da margem para procedimento longo
# sem cortar no meio da frase. E a unica trava real contra "liste tudo / repita
# N vezes". Se aparecer finish_reason=length recorrente na telemetria, suba aqui.
LLM_MAX_TOKENS=1400
# Teto diario POR consumidor: nome:limite,outro:limite. So quem for listado
# tem teto proprio; os demais respondem ao global acima. Existe por causa da
# demo, cuja chave e publica no HTML (ver DEMO_CONSUMIDOR).
RATE_LIMIT_DIARIO_POR_CONSUMIDOR=demo:200

# --- Demo web (Sprint 3) ---
# Serve app/static/index.html em /demo (e redireciona / para la).
DEMO_ENABLED=true
# Nome, dentro de API_KEYS, da integracao que a demo usa. A chave e injetada no
# HTML pelo servidor, entao ela e publica para quem abrir o DevTools — por isso
# e uma integracao PROPRIA, com teto diario proprio acima: revoga-la ou estourar
# o teto dela nao afeta o AVA. Cadastre `demo:<chave>` em API_KEYS.
DEMO_CONSUMIDOR=demo

# Serve app/static/revisao.html em /revisao: dashboard da telemetria do canal
# `eval` + conferencia manual da FIDELIDADE de cada resposta (o que a taxa de
# `acertou` nao mede). Le a `telemetria` e a `exemplo_perguntas`, nao chama
# /v1/ask. Deixe false em producao.
REVISAO_ENABLED=true

# --- CRUD do dataset de avaliacao (exemplo_perguntas) ---
# Expoe /v1/perguntas. Leitura para qualquer integracao autenticada; escrita
# (POST/PATCH/DELETE) so para o consumidor abaixo. Deixe false em producao.
PERGUNTAS_CRUD_ENABLED=true
# Nome, dentro de API_KEYS, da integracao que pode ESCREVER no dataset de
# avaliacao. Nunca use `demo` aqui: a chave da demo e publica no HTML.
# Cadastre `avaliacao:<chave>` em API_KEYS.
PERGUNTAS_CONSUMIDOR_ESCRITA=avaliacao
